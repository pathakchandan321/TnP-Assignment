1.String Compression implement a method that compresses a string using
the counts of repeated character . example : "aaabbcccc"->"a3b2c4".
if compressed string is not smaller,return original.

2.Spiral Matrix Traversa Given a 2D matrix,print all elements in spiral
order {clockwise from outside to inside}.

3.Water trapping Problem Given an array represent height of bars, calculate
how much water can be trapped after raining .Example:
        [3,0,2,0,4] can trap 7 unit of water.

4.Next Greater Element for each element in an array,find the next greater
element to its right.If none exists return -1.Example:
[4,5,2,10]->[5,10,-1]

5.Longest Substring Without Repeating Characters Find the length of the longest
substring without repeating characters.
Example: "abcabcbb"->3 "abc"

6.Stock Buy-Sell Problem Given an array of stock prices Find the
maximum profit you can achieve by buying once and selling once.
Example:[7,1,5,3,6,4]->profit=5

7.First Non Repeating Character find the first character in a string
that does't repeat. Example:"leetcode"->'i',"loveleetcode"->'v'

8.Kadane's Algorithm find the maximum sum of a contiguous subarray.
Example:[-2,1,-3,4,-1,2,1,-5,4]->6{subarray{4,-1,2,1}}

9.Balanced parentheses check if a string containing brackets is balanced.
Example: "{[()]}"->valid,"{[(])}"->invalid.